{
  "name": "emby-probe-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency backend for the Emby Probe frontend.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
